class imgClass{
  constructor(img,x,y,w,h){
    this.x=x;
    this.y=y;
    this.w=w;
    this.h=h;
    this.img=img;
    // this.windowWidth=windowWidth;
    // this.windowHeight=windowHeight;
  }

  show(){

    // for (let this.x=0; x<this.windowWidth/10; this.x++){
    //   for (let this.y=0; this.y<this.windowHeight/10; this.y++){
    //       image(this.img, 10 + this.x * this.x, 10 + this.y * this.y,this.w,this.h);
    // // }

    image(this.img,this.x,this.y,this.w,this.h);

    }
  }

    // render(){
    //   for(let i=0;i<animation.length;i++){
    //
    //     image(images[i],)
    //   }
  // }



// image(img, x, y, [width], [height])
